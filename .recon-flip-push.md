# Recon: Verify Flip push-keeps-teams rule

Pre-test verification pass — no on-course bug observed yet. Confirming
the canonical rule from PR #16 still holds in current code.

## Canonical rule

When a hole pushes in Flip base game (1-15), teams stay the same on
the next hole. No reshuffle. The carry-over pot accumulates
independently. Reshuffle only happens after a decided hole (one team
wins). Hole 16 transitions to crybaby phase regardless.

## Plan

1. Engine: `advanceFlipState` writes prior teams into next slot on push
2. Runtime: `getTeamsForHole` reads from `flipState.teamsByHole`
3. UI: per-hole Flip button hidden/disabled after push
4. Carry-over: pot growth independent of team identity
5. Tests: explicit + implicit coverage audit

This file is a placeholder seeding the recon branch / PR. Will be
deleted with the recon comment on close.
